# Copy to .env.local for local development against a separately-running
# backend (no trailing slash on the URL).
VITE_API_URL=http://localhost:4000

# For a production build served by the Node app itself (npm run build at the
# repo root), leave VITE_API_URL unset or set it to an empty string so API
# calls are same-origin instead of pointing at localhost.
