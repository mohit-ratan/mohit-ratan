{
  "name": "packsomework",
  "version": "1.0.0",
  "private": true,
  "description": "PackSomeWork — Express API + React frontend served from a single Node app (auth with email verification/OTP, posts, stories, profiles).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "mysql2": "^3.10.0",
    "nodemailer": "^10.0.10",
    "uuid": "^14.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
